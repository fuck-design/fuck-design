import React from 'react';
import { HrProps } from './types';

export { HrProps };

/**
 * Pre 组件
 * @param props
 * @returns
 */
export const Hr: React.FC<HrProps> = (props: HrProps) => {

  return <hr />;
};
